version: '2'

output: prefixed
silent: true

vars:
  pwd:
    sh: pwd
  random_uuid:
    sh: cat /proc/sys/kernel/random/uuid

tasks:
  generate:
    sources:
      - '*.go'
    generates:
      - '*_gen.go'
    cmds:
      - go generate

  build:
    desc: build go code
    deps: [generate]
    cmds:
      - go build

  reset:
    cmds:
      - rm -f conf/token conf/sshkey conf/sshkey.pub

  rr:
    cmds:
      - task: reset
      - task: build
      - SNAKE_NAME={{ .random_uuid }} ./snake-runner -c ./conf/snake.dev.conf
